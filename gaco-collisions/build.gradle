ext {
    kibuModules = [
            'kibu-hook-api',
            'kibu-hooks',
            'kibu-scheduler-api',
    ]
    gacoModules = [
            'gaco-math',
            'gaco-ds',
    ]
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'aco-collisions'
                description = 'API for detecting and handling collisions / movement'
            }
        }
    }
}
